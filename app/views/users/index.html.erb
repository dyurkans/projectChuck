<div class="row">
    <div class="span10">
          <h3>Users</h3>
          <%= render :partial => "users" %>
    </div>
</div>

<%= link_to raw("<span class='glyphicon glyphicon-user'></span> Add a User"), new_user_path, :class => 'btn btn-primary', :id => 'Create User' %>