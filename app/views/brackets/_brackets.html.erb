<% if @brackets.empty? %>
  &nbsp;&nbsp;<em>None at this time</em>
<% else %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Ages</th>
  			<th>Gender</th>
  			<th>Number of Teams</th>
        <th>&nbsp;</th>
      </tr>
    </thead>
    <tbody>
      <% @brackets.each do |bracket| %>
        <tr>
          <td><%= link_to "#{bracket.min_age} - #{bracket.max_age}", bracket_path(bracket) %></td>
  				<td><%= bracket.sex %></td>
          <td><%= bracket.teams.size() unless bracket.teams.nil? %></td>
          <td><%= link_to raw("<i class='icon-pencil'></i> Edit"), edit_bracket_path(bracket), :class => 'btn btn-mini' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <div class="digg_pagination">
    <div clas="page_info">
      <%= page_entries_info @brackets %>
    </div>
    <%= will_paginate @brackets, :container => true, :previous_label => "Previous", :next_label => "Next", :inner_window => 2 %>
  </div>
<% end %>